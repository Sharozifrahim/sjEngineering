{
  "name": "sj-engineering-website",
  "version": "1.0.0",
  "private": true,
  "description": "SJ Engineering — 3D animated website with product viewer and AI assistant, bundled with Parcel.",
  "scripts": {
    "start": "parcel index.html product.html --port 1234",
    "build": "parcel build index.html product.html equipment.html --dist-dir dist && copy sitemap.xml dist\\ && copy robots.txt dist\\"
    "clean": "rimraf .parcel-cache dist"
  },
  "dependencies": {
    "three": "^0.160.0"
  },
  "devDependencies": {
    "parcel": "^2.12.0",
    "rimraf": "^5.0.5"
  },
  "browserslist": [
    "defaults"
  ]
}
